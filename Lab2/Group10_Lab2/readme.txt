To compile the program , run the command :
    g++ group10_lab2.cpp

Now run a command with command line argument as file of the input file : 

    ./a.out input.txt

This will generate a file output.txt which consists of the stats of the performance of algorithms.

->For running second input file rename it as input.txt